require "spec_helper"

RSpec.describe Cuba::Router do
  it "has a version number" do
    expect(Cuba::Router::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
